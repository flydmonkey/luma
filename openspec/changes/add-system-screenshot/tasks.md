## 1. 去掉游戏录制

- [x] 1.1 主页模式行去掉游戏格，设置和模式列表里也不再提供游戏录制。已保存的游戏模式在启动后回到显示器模式。打开主页和设置，确认没有游戏入口
- [x] 1.2 主页模式行改为显示器、区域、窗口、截图、录音。截图格点下去不改变当前录制模式。先选中窗口模式再点截图，确认窗口模式仍选中
- [x] 1.3 局域网网页和接口不再列出游戏目标，也不接受游戏模式开录。请求目标列表时没有游戏项；用游戏模式调用开始接口时失败且不产生文件

## 2. 静止图

- [x] 2.1 按虚拟屏幕物理像素拷出矩形。保存时写成保存目录中的 `Luma-Shot-yyyyMMdd-HHmmss.png`，且不改剪贴板。用一张已知矩形核对文件尺寸，并确认剪贴板仍是截图前的内容
- [x] 2.2 复制时把同一张图放入剪贴板，且不写 PNG。在可写目录下点复制，确认没有新 PNG，粘贴得到该图
- [x] 2.3 小于 8×8 物理像素时关掉遮罩，不出现保存和复制，也不写文件。用边界尺寸测试确认
- [x] 2.4 保存失败时提示且不报成功，剪贴板不变。复制失败时提示，不写 PNG，不报成功。分别用拒绝写入的目录和剪贴板失败路径验证

## 3. 选区遮罩

- [x] 3.1 每台显示器一扇变暗遮罩，默认矩形拖拽。拖出有效区域后，选区旁出现保存和复制。Esc 和未形成区域的拖拽关掉全部遮罩，不写 PNG，不改剪贴板。在主显示器上拖拽、点保存、再按 Esc 各做一次
- [x] 3.2 窗口模式高亮指针下的可见顶层窗口，点击后在该窗口外接矩形旁出现两个按钮。点一个已知窗口再点保存，核对 PNG 范围
- [x] 3.3 整屏模式点击哪台显示器，就在该显示器范围旁出现两个按钮。点击主显示器再点复制，核对粘贴图尺寸
- [x] 3.4 遮罩显示期间设置从捕获中排除。用窗口样式或捕获亲和性查询确认标记已加上

## 4. 入口

- [x] 4.1 截图热键出厂为 Ctrl+Shift+S，跟随现有全局热键开关，不注册 Win+Shift+S。设置缺少该键时用出厂值。恢复默认设置写回 Ctrl+Shift+S。用缺键的设置文件、热键开关关闭、以及恢复默认后的设置文件验证
- [x] 4.2 托盘菜单和浮动录制条不增加截图项。录制中用热键打开遮罩再取消，确认会话仍在、主窗口仍隐藏、浮动条仍在。对照托盘菜单和浮动条上的按钮
- [x] 4.3 五语表补上截图格、保存、复制、热键标签和失败提示，并去掉游戏模式文案。切换五种界面语言，确认这些字符串都不是缺键原文

## 5. 联动核对

- [x] 5.1 录制中点保存得到的 PNG 不出现在片库网格，录制输出文件不变。录制中保存一张图再停止，核对两个文件。点复制的那次不产生 PNG

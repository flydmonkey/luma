## 1. 仓库脚手架与许可证

- [x] 1.1 用 WinUI 3 / .NET 8 / x64 自包含模板创建 `Luma.sln` 与 `src/Luma.App`，窗口标题为 Luma，并确认 `dotnet build src/Luma.App/Luma.App.csproj -c Debug -p:Platform=x64` 成功
- [x] 1.2 添加 `src/Luma.Core`、`src/Luma.Obs`、`src/Luma.Media`、`tests/Luma.Core.Tests`、`tests/Luma.Obs.Tests` 空项目与项目引用，并确认解决方案一次 `dotnet test` 能发现测试项目
- [x] 1.3 写入 GPL-2.0 兼容的 `LICENSE`、`COPYING` 占位和 `ThirdPartyNotices` 模板，并确认仓库根目录能看到这些文件且文案声明链接 libobs
- [x] 1.4 添加 `.gitignore`（bin/obj、OBS 运行时缓存、用户设置）和最小 README，并确认 `git status` 不再把构建输出当源文件

## 2. 桌面空壳与设置

- [x] 2.1 实现主窗口三页导航（录制 / 我的视频 / 设置），启动默认落在录制页，并确认切换页时不会打开第二个主窗口
- [x] 2.2 实现本地设置存储（主题默认暗色、语言默认跟随系统、保存目录、硬件编码开关、LAN 端口默认 `12345` 等），并确认改保存目录和 LAN 端口后重启仍读到原值
- [x] 2.3 实现设置页外观、文件、声音、画质分组控件，并确认恢复默认会把主题打回暗色、语言打回跟随系统
- [x] 2.4 实现单实例启动，并确认第二次启动不会再开一个录制进程而是唤起已有实例

## 3. libobs 宿主最小闭环

- [x] 3.1 钉扎一组官方 OBS Studio Windows x64 运行时，编写拉取/裁剪脚本（只要采集、编码、封装插件和 data），并确认脚本能在干净目录放出 `obs.dll` 且不含 OBS 主界面可执行文件
- [x] 3.2 新建 `native/Luma.ObsHost`，完成 `obs_startup`、模块加载和一个内部 scene，不链接 `obs-frontend-api`，并确认无头启动后进程能保持运行且不弹出 OBS 窗口
- [x] 3.3 实现 named pipe 会话协议的 `Start/Stop/GetStatus`（显示器 + 可选系统声 → MP4），并确认用最小客户端能打出可播放的 `.mp4`
- [x] 3.4 实现 GPU 编码器探测（NVENC / AMF / QSV）与 `obs_x264` 回退，并确认 `GetStatus` 返回真实编码器名而不是字面量 `hardware`
- [x] 3.5 实现 `src/Luma.Obs` 客户端与 `IRecordingEngine`，宿主崩溃时会话失败可恢复，并确认杀掉 ObsHost 后 UI 进程仍在且能再次 Start
- [x] 3.6 用 OBS output 的已编码时长/skipped 填充会话状态，并确认停止后报告时长与文件时长相差不超过 1 秒

## 4. 五种模式与录制交互

- [x] 4.1 在录制页做出五种模式、系统声/麦克风快捷开关和清晰度选择，并确认纯音频模式的摘要写明只录音、输出为音频文件
- [x] 4.2 实现显示器 / 区域 / 窗口 / 游戏目标选择（区域橡皮筋、窗口与游戏列表），无目标时开始按钮不可用，并确认未选目标时不会生成文件
- [x] 4.3 把区域 crop、窗口、游戏参数交给 ObsHost 对应 source，游戏独占全屏失败要明确报错，并确认窗口被遮挡时成片仍是目标窗口内容
- [x] 4.4 实现手动开录 3 秒倒计时、取消倒计时、主窗避让与自录排除，并确认手动开录的成片里看不到 Luma 主窗口
- [x] 4.5 实现单会话开始 / 暂停 / 恢复 / 停止，停止后留在录制页并冻结计时，并确认暂停区间不会出现在成片里
- [x] 4.6 实现浮动录制条（时长、暂停、停止、麦静音、排除采集），并确认静默模式下由 LAN/API 开录时不出现该条
- [x] 4.7 实现 WASAPI 系统声与麦克风独立开关及设备缺失提示，并确认五分钟成片音画漂移不超过 1 秒
- [x] 4.8 实现纯音频会话写出 `.m4a`，并确认片库出现的是音频项而不是视频项

## 5. 叠加、自动化与托盘热键

- [x] 5.1 通过 ObsHost 叠加摄像头、文字、图片和时间戳水印，默认无品牌水印，并确认未配置水印的成片没有 Luma/OBS 字样
- [x] 5.2 实现登录后开录、定时开停、按时长或大小分段，并确认 10 分钟分段会留下可播放的第一段并自动接下一段
- [x] 5.3 实现全局热键、托盘图标、关闭进托盘、登录进托盘、隐藏托盘图标，并确认热键能在主窗未聚焦时停止录制
- [x] 5.4 实现异常退出留下可修复文件，并确认强杀进程后保存目录里仍有部分文件

## 6. 片库与后期

- [x] 6.1 实现我的视频列表（缩略图、名称、大小、时长、日期）和空状态回录制，并确认停止录制后无需手动刷新就能看到新文件
- [x] 6.2 实现预览、重命名、打开目录、确认删除和改保存目录，并确认删除需确认且确认后文件从磁盘消失
- [x] 6.3 接入随包 FFmpeg 的修复与压缩（默认另存），并确认压缩后原片仍可播放
- [x] 6.4 实现剪切、合并、字幕和配乐导出（默认不覆盖原片），并确认非法剪切时间范围会被拒绝且说明原因
- [x] 6.5 确认后期任务不会接管正在进行的 ObsHost 会话：开录中压缩旧文件时，当前录制仍继续

## 7. 局域网控制

- [x] 7.1 实现 LAN 开关、可选访问密钥，以及桌面可改的监听端口（出厂 `12345`，合法范围 1–65535），并确认默认地址为 `http://127.0.0.1:12345`、关服后面页和 API 都连不上
- [x] 7.2 实现端口校验与占用提示：拒绝 `0` / `123456` / 大于 65535 的值，占用时明确报错且不换端口；网页和 API 不能改端口，并确认改合法空闲端口后设置页地址和实际绑定一致
- [x] 7.3 实现与桌面同构的三页 Web（录制 / 我的视频 / 设置），网页设置不能关 LAN、改端口或回传真实密钥，并确认解锁后能在三页间切换
- [x] 7.4 实现 `/api/v1` 会话、目标列表（含标签和预览）、设置读写和片库操作（删除需 confirm），并确认无目标时 start 失败且不落文件
- [x] 7.5 用手机或另一台机器走一遍开停录制，并确认成片只出现在宿主电脑保存目录

## 8. 本地化与对照验收

- [x] 8.1 补齐英 / 简中 / 繁中 / 日 / 韩文案，设置页切换语言无需重装，并确认跟随系统且 OS 语言不在五语内时界面为英语
- [x] 8.2 对照 `record` 走完「选模式 → 开录 → 停录 → 预览 → 设置」主路径，并确认没有引入 Media Foundation 自研硬编路径
- [x] 8.3 跑 `dotnet test` 与一次本机硬编显示器录制，并确认会话里的编码器名、有效帧率和文件时长一致
